import React from 'react';
import { useMutation } from '@apollo/client';

import SportSelection from '../../components/UI/SportSelection';

const editAthlete = () => {
    

    return (
        <SportSelection />
    );
};

export default editAthlete;